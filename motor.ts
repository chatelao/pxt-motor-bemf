//% color=270 weight=100
namespace motor {
    /**
     * This is a C++ function that adds two numbers
     */
    //% block="sum of %a and %b"
    //% shim=motor::sum
    export function sum(a: number, b: number): number {
        // This part is ignored when a `shim` is present
        return 0;
    }
}
